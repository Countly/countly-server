# Kafka Configuration - Sensitive Settings
# These are the credentials and connection details that should be kept secret
# 
# ⚠️  IMPORTANT: When Kafka is enabled, it becomes a HARD DEPENDENCY.
# Any connection failures will cause the application to exit.

# Kafka broker addresses (sensitive as they expose internal infrastructure)
COUNTLY_CONFIG__KAFKA_RDKAFKA_BROKERS=["kafka:9092"]

# Security settings - choose ONE security protocol based on your Kafka setup

# Option 1: PLAINTEXT (development only - not secure)
# COUNTLY_CONFIG__KAFKA_RDKAFKA_SECURITYPROTOCOL=PLAINTEXT

# Option 2: SSL/TLS only
# COUNTLY_CONFIG__KAFKA_RDKAFKA_SECURITYPROTOCOL=SSL
# Additional SSL configuration would go in separate mounted certificates

# Option 3: SASL with PLAINTEXT (credentials but no encryption)
# COUNTLY_CONFIG__KAFKA_RDKAFKA_SECURITYPROTOCOL=SASL_PLAINTEXT
# COUNTLY_CONFIG__KAFKA_RDKAFKA_SASLMECHANISM=PLAIN
# COUNTLY_CONFIG__KAFKA_RDKAFKA_SASLUSERNAME=your-kafka-username
# COUNTLY_CONFIG__KAFKA_RDKAFKA_SASLPASSWORD=your-kafka-password

# Option 4: SASL with SSL/TLS (recommended for production)
# COUNTLY_CONFIG__KAFKA_RDKAFKA_SECURITYPROTOCOL=SASL_SSL
# COUNTLY_CONFIG__KAFKA_RDKAFKA_SASLMECHANISM=SCRAM-SHA-256
# COUNTLY_CONFIG__KAFKA_RDKAFKA_SASLUSERNAME=your-kafka-username
# COUNTLY_CONFIG__KAFKA_RDKAFKA_SASLPASSWORD=your-kafka-password

# Custom transactional ID prefix (optional, for avoiding collisions)
# COUNTLY_CONFIG__KAFKA_TRANSACTIONALID=countly-producer