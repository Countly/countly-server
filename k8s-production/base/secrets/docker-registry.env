# Docker Registry Configuration for Image Pulling
# Base64 encoded .dockerconfigjson for accessing private container registry
# Generate this by running: kubectl create secret docker-registry docker-pull-secret --docker-server=gcr.io --docker-username=_json_key --docker-password="$(cat service-account-key.json)" --dry-run=client -o yaml | grep .dockerconfigjson | cut -d':' -f2 | xargs
DOCKER_CONFIG_JSON=PLACEHOLDER_REPLACE_WITH_BASE64_ENCODED_DOCKERCONFIGJSON