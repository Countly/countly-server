/**
 * Base Job class for creating scheduled jobs
 * @module jobServer/Job
 *
 * Proxy file - re-exports from TypeScript implementation
 */

const Job = require('./Job.ts').default;

module.exports = Job;
