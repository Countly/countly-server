/**
 * PulseJobRunner - Consolidated Pulse job runner
 * @module jobServer/PulseJobRunner
 *
 * Proxy file - re-exports from TypeScript implementation
 */

const PulseJobRunner = require('./PulseJobRunner.ts').default;

module.exports = PulseJobRunner;
