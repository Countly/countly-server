var plugin = {};
//var countlyConfig = require("../../../frontend/express/config");

plugin.init = function(/*app, countlyDb*/) {
    //add middleware here
    /*app.use(countlyConfig.path + '/dashboard', function(req, res, next) {
        next();
    });*/
};

module.exports = plugin;