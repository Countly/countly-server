var pluginInstance = {};

(function(plugin) {
    // eslint-disable-next-line no-unused-vars
    plugin.init = function(app, countlyDb) {
        // Plugin initialization - routes and menu are registered in countly.views.js
    };
}(pluginInstance));

module.exports = pluginInstance;

