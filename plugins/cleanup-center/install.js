#!/usr/bin/env node

/**
 * Cleanup Center install script (core copy).
 *
 * This mirrors the plugin entry under `plugins/cleanup-center/install.js`
 * and exists so that any tooling referring to the core plugins directory
 * can run the install phase without errors.
 */

console.log("cleanup-center (core): install script running (no-op).");
process.exit(0);



